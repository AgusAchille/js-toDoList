//Usuario haciendo click en el add button
//si hay texto en el campo de texto entonces se lo agrega a la lista
document.getElementById('add').addEventListener('click', function(){
    let value = document.getElementById('item').value;
    
    //Si el valor existe (si tiene algo escrito)
    if(value==true)
        console.log(value)

});
